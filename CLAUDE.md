# 語灯 (gotou) — 浄土宗大辞典ビューアプリ

浄土宗大辞典(https://jodoshuzensho.jp/daijiten/ 、MediaWiki製・約1万項目)を
スマートフォンで引けるようにする辞書アプリ。PWAとして開発し、将来Capacitorでストア展開する。
開発者は一人(浄土宗僧侶・Python経験者)。実装は主にClaude Codeが担う。

## 絶対の掟(違反はインシデント)

1. **大辞典由来のデータをコミット・公開しない。** 対象: クロール結果、キャッシュ、
   生成したSQLite/.Dictionary、項目本文の断片を含むスナップショットすべて。
   - 公開してよいデータは2つだけ: `shared/gongyo/`(パブリックドメインの経文等)と
     `factory/tests/fixtures/`(自作のダミー項目)。
   - 迷ったらコミットせず停止して確認を求めること。
2. **許諾を得るまで「浄土宗大辞典」をアプリ名・説明文に名乗らない。**(公式と誤認されるため)
3. **取得は行儀よく。** レート制限・キャッシュ再利用を必ず守る(詳細は factory/CLAUDE.md)。

## リポジトリの地図

- `factory/` … Pythonデータ工場。MediaWiki → SQLite / .Dictionary 変換
- `app/` … TypeScript/React PWA(将来Capacitor)
- `shared/` … **両者の契約。ここだけが接点**
  - `schema.sql` … SQLiteスキーマの正(変更時はfactory/appを同時修正)
  - `variants.json` … 異体字・旧字体の正規化テーブル(Pythonはキー生成、TSは入力正規化で同じものを読む)
  - `gongyo/` … 勤行テキスト(PD原文)と差定プリセット
- `docs/` … 設計文書・決定の経緯。仕様の詳細はここを参照

**factoryとappは互いに直接依存しない。** shared/ の契約だけを見ること。

## モデル運用

- 実装はSonnetで行う。
- **同一のエラー・テスト失敗に2回連続で修正失敗したら、自力で続けず debugger
  サブエージェント(Opus)に調査を委譲する。**
- アーキテクチャ判断・スキーマ変更のような設計仕事はプランモードで計画してから着手する。

## 作業の流儀

- 変更は小さく。1コミット=1目的。コミットメッセージは日本語でよい。
- スキーマ(`shared/schema.sql`)や `variants.json` を変えるPRは、必ず両側(factory/app)の追随を含める。
- 仏教用語・宗門の慣習に関わる仕様(差定、勤行の作法など)は勝手に推測せず、
  docs/ を確認するか開発者(僧侶)に質問する。

## 最小用語集

- **読み(よみ)** … 見出し語のかな表記。検索の主キー。仏教語は音読み(呉音)が多い
- **差定(さじょう)** … 勤行の式次第。「テキスト単位=曲、差定=プレイリスト」のモデルで扱う
- **勤行(ごんぎょう)** … おつとめ。app/ の勤行モードが対応する
- **異体字** … 彌/弥、佛/仏 など。検索時に variants.json で吸収する
